# Hooks (enforced outside the model)

Hooks and permissions are machinery, not prose: `graph/apply-guards.js` deploys them,
`node graph/apply-guards.js diagnose` lists what is live. Never hand-edit settings.json.
