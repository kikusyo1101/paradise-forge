## 神託
> 改善を続けてください。少なくとも3回はループしてください

**このPRには 第27条 と 第28条 の二つの改革が含まれる。**
同じブランチに積んでしまったためである（本来は別PRにすべきだった。判断の誤りを認める）。
神が個別に裁けるよう、両方を以下に記す。

---

# 【ループ1】第27条 — 成果物は誰がやったかを証明しない

## 🔍 これは化粧の問題ではなかった

`contract.js` は成果物の**実在と大きさ**だけを検めていた。

```js
if (!exists) return { accepted: false, ... };
if (size < minBytes) return { accepted: false, ... };
return { accepted: true, verified: 'file' };   // ← 誰が作ったかを問わない
```

**教主が己の手で書いた成果物も、この二条件を完璧に満たす。**
つまり照合機構は**委譲と成りすましを区別できなかった**。

11件のPRが枢機卿を一度も召集せずに生まれた根が、まさにここにある。
第25条で階層に実体を与え、第26条で安全な幅を与えてなお、
**「本当に発令されたのか」を確かめる手段が無かった。**

### 調査が名指ししていた

> **MAST (arXiv:2503.13657)** — FM-2.6「**推論と実行の不一致**」**13.98%**
> 「委譲する」と述べながら自分で実行する挙動。計測されている。

> **Claude Agent SDK docs**
> 「`tool_use.name in ("Task","Agent")` を検出し、子のメッセージが `parent_tool_use_id` を
> 持つことを確認する。**これが実体があるかを検証する唯一確実な手段である**」

---

## 🔧 第27条 — 起動の証跡

`spawn-trace.js` を新設。起動を**三値**で裁く:

| 状態 | 意味 | 判定 |
|---|---|---|
| `observed` | `tool_use` の実idがある | ✅ **唯一の緑** |
| `asserted-only` | 「起動した」という自己申告のみ | ❌ 主張は証拠ではない(第5条) |
| `no-trace` | 証跡なし | ❌ 自分でやった可能性を否定できない |

`contract.reconcile` は走行状態を渡されたとき証跡を照合し、
起動されていない相の成果物を `file-but-unspawned` として**拒否**する。

**後方互換**: 走行状態を渡さない既存の呼び出しは従来どおり動く（回帰テストで固定）。

---

## 🧪 証拠 — 門をわざと壊して四段階で確認

同一の完璧な成果物に対し、状態だけを変えて実行:

```
① 走行状態なし（従来の呼び出し）
   accepted: true,  verified: "file"                    ← 後方互換

② 走行状態あり・証跡なし（教主が自分でやった状態）
   accepted: false, verified: "file-but-unspawned"
   「この相には起動の証跡が一つも無い — 誰かが自分でやった可能性を否定できない」

③ 自己申告のみ（tool_use idが無い）
   accepted: false, verified: "file-but-unspawned"
   「『起動した』という自己申告だけで、tool_use の証跡が無い。主張は証拠ではない(第5条)」

④ 観測された起動
   accepted: true,  verified: "file+spawn"
   「artifact verified (28b) and dispatch observed: 1 件の起動を観測: market-researcher」
```

**③が核心である。** 自己申告を証拠として認めないことが、素通りを塞ぐ。

```
Paradise self-test: 168 passed, 0 failed          (163 → 168)
check-agents / census / apply-models / apply-spawn / deploy / branch-guard : 全 exit=0
素の環境(CI相当): 168 passed, 0 failed
```

**CIステップは実際に走らせて確認した**（YAMLが妥当なことと、中身が動くことは別である）:
```
$ bash <抽出したCIステップ>
spawn trace gate fires correctly on all three states
exit=0
```

---

## 憲法照合

- **第5条**（主張は証拠ではない）: 自己申告の起動を明示的に不合格とした
- **第9条**: 監査対象は創造物でなく楽園自身の機構
- **第21条**: 発見は必ず**どの相か**を名指しする（`bypassed` が相名を返す）
- **第27条**: 本PRで制定

## 正直に述べる限界

- **`spawn-trace` は証跡を記録する器であり、記録する主体は依然として教主である。**
  教主が意図的に偽の `toolUseId` を書けば通ってしまう。真に外部から観測するには
  Claude Code のセッションログを直接読む必要があり、本PRの範囲を超える
- ただし**穴は一段深くなった**: 以前は「何もしなくても通る」、今は「偽証しなければ通らない」

## 裁き
執行官(CI)と神の御手に。教主は自らを承認しない。


---

# 【ループ2】第28条 — 規範の教訓は grep で裁けない

## 🔍 「2件の gap」の正体は、門の設計欠陥だった

`critic.js review graph --self` は 2件の LESSON REGRESSION を出し続けていた。

```
🔴 lesson:browser-cleanup: 「目視検証でブラウザを開いたら必ず閉じる」not addressed
🔴 lesson:late-research-still-rules: 「遅れて届いた証拠にも同じ効力がある」not addressed
```

これを「直すべき欠陥」と読むのは誤りだった。**両方とも行いの掟であり、
コードに文字列として現れようがない。** critic は全教訓を同じ方式で裁いていた:

```js
return looseIncludes(hay, needle)      // ← コードに単語が現れるか
  ? { ok: true,  note: 'lesson satisfied' }
  : { ok: false, note: 'LESSON REGRESSION' };
```

### 実測で判明した、遥かに重い事実

```
$ node -e "教訓30件を機構/規範に分類"
機構: 12   規範: 18
```

**30件中18件が規範。だが赤かったのは2件だけ。**

つまり残り16件は「守られていた」のではなく、**偶然コードに単語が現れただけ**だった。
判定は機能しておらず、**偶然に依存していた**。
2件の不運な赤が本物の欠陥に見え、16件の幸運な緑が真の盲目を隠していた。

---

## 🔧 第28条 — 測れぬものも憶えねばならぬが、点数を付けてはならぬ

教訓に `kind` を持たせた（既存の `|applies:` と同じ流儀）:

| kind | 裁き方 |
|---|---|
| `mechanism` | 従来どおり成果物と照合（実装が現れるべきもの） |
| `conduct` | **提示のみ。点数を付けない**（行いの掟） |

**二つの誤りは等価である:**
- 永久に赤 → 読まれなくなる。**無視される門は、無い門より悪い**（第21条の教訓）
- 緑にして黙らせる → 教訓が消える

正しい扱いは**毎回の審査で立ち続ける義務として提示すること**。

```
✓ lesson:browser-cleanup: CONDUCT — 実行時に守るべき掟（コードでは検証できない）: …
✓ lesson:late-research-still-rules: CONDUCT — …
VERDICT: the critic found nothing. Proceed to judgment.
```

**未宣言は `mechanism` に倒れる** — 既存31件の裁き方を変えない（後方互換）。

---

## 🧪 証拠

```
Paradise self-test: 172 passed, 0 failed          (168 → 172)
critic.js review graph --self : exit 0            ← 初めて緑になった
check-agents / census / apply-spawn / deploy / branch-guard : 全 exit=0
素の環境(CI相当): 172 passed, 0 failed
```

**門をわざと壊して確認**:
- 教訓の語が一切現れないコードに対し、`conduct` は緑・`mechanism` は赤になることを検証
- `|kind:` マーカーが `check` 本文へ漏れないことを検証（漏れればその文字列を
  コードから探すことになり本末転倒）

### 途中で自分の誤りを見つけた（正直な記録）

テストが最初落ちた。原因は私の思い込みで、`critic.review(dir, {lessons})` の
`lessons` は**配列ではなくファイルパス**だった。実装を読んで確かめ、修正した。
「動くはず」で書いたテストが、実装を読ませてくれた。

---

## 憲法照合

- **第21条**（無視される門は無い門より悪い）: 本条の直接の根拠
- **第22条**（数えられぬものは主張しない）の対**: 数えられぬものをどう扱うかを定めた
- **第28条**: 本PRで制定

## 正直に述べる限界

- **`conduct` 教訓が実際に守られたかは、依然として誰も検証していない。**
  提示するだけである。守らせるには実行時の観測（第27条の証跡のような）が要り、
  行動によっては観測手段自体が存在しない
- 既存29件は `mechanism` のまま。**16件は今も偶然で緑になっている可能性がある** —
  一件ずつ性質を見直す作業は本PRの範囲外とした

## 裁き
執行官(CI)と神の御手に。教主は自らを承認しない。
