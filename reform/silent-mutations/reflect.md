# reflect — reform 走行 `silent-mutations` の自己批評

**書く者**: self-critic(独立した断罪機関 / 第11条 —— どの枢機卿にも属さない)
**日付**: 2026-09-17
**姿勢**: **この走行が自分について語っていることを、私は疑う。**
走行帳の主張を引き写さない。**疑う主張は自分で撃ち直す。**
実験はすべて複製 `$LOCALAPPDATA/Temp/sc/paradise`(`.git` / `node_modules` を除く全複製)+
実台帳の写しを置いた偽の兄弟倉に対して行った。**実装は一行も直していない。commit していない。**

```
[開始] $ sha256sum ../paradise-creations/gauge-ledger.jsonl
955ea34a020bf5681b5607de9ed22abaddc7bc5228cb231dd843c05ba55aab77   7 行
[全実測の後]
955ea34a020bf5681b5607de9ed22abaddc7bc5228cb231dd843c05ba55aab77   7 行
```

**全 15 回の変異走行の各回で、実台帳の sha256 を機械が照合した(`realLedgerUnchanged=true` を毎回印字)。**
書き戻しは毎回生バイト列で行い、`restored=true` を印字した。`git checkout -- <file>` は一度も使っていない。

---

## 1. 神託への直答

神託は二つだった。**歯切れ良く答える。**

### GAUG-18「TOCTOU 未実測」 → **片付いた。**

神託の文言は **「未実測」** である。走行は 150 試行を撃ち、**150/150 破れる**という数を出した。
窓の幅(10〜20ms)、破れ方が一種類だけであること(重複行の生成のみ / `skipped` の誤報は 250 回の機会に 0 回)、
`foldLedger` が読み手を守ること —— すべて数が出ている。**「未実測」は解消された。**

**α(ロック)を却下した裁定は正しい。** 根拠は三つとも実測か条文に立っている:
第55条(e) の fail-open と正面衝突すること、Node 標準に `flock` が無いこと、
そして**記録が失われる方向には一度も倒れなかった**という 250 回分の実測。
**測っていない代償(ロックの待ち時間)を、測った実害(ゼロ)のために払わない** —— 第38条そのものである。

神が「直せ」と望んだのなら神託は「TOCTOU を直せ」と書かれたはずである。
**「未実測」と書かれた神託に対し、測り、測った結果に基づいて直さないと裁定し、その裁定の根拠を数で書いた。これは満額回答である。**
ただし PR.md §1 の表が `TOCTOU` の「後」欄に **「同じく破れる」** と書いているのは正しい。隠していない。

### GAUG-10「無音の変異を潰す」 → **半分だけ片付いた。潰れていない層が測定で残っている。**

私が自分で撃ち直した結果:

| findings が【致命】と呼んだ 4 件 | 私の実測 | 判定 |
|---|---|---|
| **S1**(`epochStatus` の序列判定を潰す) | 🔔 **CAUGHT** exit=1 red=3 | **塞がった** |
| **S4**(`TIER_EPOCH_AT` を 2099 に) | 🔔 **CAUGHT** exit=1 red=3 | **塞がった** |
| **T9**(`preemptionReasons` が「読めない ts」を数えない) | 🔔 **CAUGHT** exit=1 red=2 | **塞がった** |
| **G8**(`record` がプロセスの記憶を信じる) | 🔔 **CAUGHT** exit=1 red=5 | **塞がった** |

**致命 4 件は 4/4 本当に塞がった。** これは走行の主張どおりであり、私は自分の手で確かめた。
**走行を褒めるべき点である。**

だが findings が【重大】と呼んだ層は、**21 門を建てた後の今日も無音である**(私の実測):

| 件 | 私が撃った変異 | 結果 |
|---|---|---|
| **B7** | `dir.isDirectory()` → `statSync(...).isDirectory()`(junction を辿る) | ❌ **SILENT** exit=0 red=0 |
| **B8** | `['conclave.json']` → `readdirSync(...).filter(n=>n.endsWith('.json'))` | ❌ **SILENT** exit=0 red=0 |
| **T7** | audit 側の未来判定だけ `CLOCK_SKEW_TOLERANCE_MS * 365` | ❌ **SILENT** exit=0 red=0 |

この三件は **SM-H / SM-I / SM-G として名を持つ**ので「見落とし」ではない。第16条は守られている。
だが**「無音の変異を潰した」という言葉が全称で読まれてはならない。潰したのは致命 4 件である。**

**そして——本走行が建てた新しい門そのものが、新しい無音層を持っている。** §2 で数を出す。

---

## 2. この走行の主張のうち、弱いもの(名指し)

### 弱い主張 ①【最も重い】 「静的の門が『門の書き方』の層を閉じた」は、**致命の実形に対して 0/2 である**

本走行の中核は **`gauge(静的)` の門**である。第62条(c) の本体であり、
無音率 40% の最悪層(findings §3-2)に対する答えとして建てられた。
security は A45 で「G4 の 4 形中 3 形が無音」を摘発し、verify は **「6/6 CAUGHT」「偽陽性 0」** と応じた。

**私はその主張を撃ち直した。** 字句器(`mutableGlobals`、`tests/paradise.test.js:3754-3858`)を
そのまま切り出して走らせ、さらに複製の全 492 門にも撃った。

```
[複製 / --gate gauge 静的 番兵 並行 門ヘルパー = 137 of 492 gates]

🔔 CAUGHT | CTRL: let 大域(設計者の形)                    | exit=1 red=1  ← 静的の門が鳴る
❌ SILENT | G4等価-a: 関数内で globalThis.__m ||= new Map() | exit=0 red=0
❌ SILENT | G4等価-b: 関数内で module.exports.__stash = {}   | exit=0 red=0
❌ SILENT | G4等価-c: 最上位で Object.assign(globalThis, {…})| exit=0 red=0
❌ SILENT | G4等価-d: 関数内で Reflect.set(globalThis, …)    | exit=0 red=0
```

**そして本命はここである。** findings が【致命】【重大】と呼んだ G8 / G6 を、
**現実に人が書く形(`globalThis` を関数の中で生やす)**で撃った:

```
G8等価(record が globalThis の記憶を信じる) | exit=1 red=5  | 静的門が鳴った = false
G7等価(score を globalThis で slug 鍵メモ化)| exit=1 red=10 | 静的門が鳴った = false
```

**どちらも赤くなったが、鳴ったのは既存の振る舞いの門であって、この層のために建てられた静的の門は一本も鳴っていない。**

> **静的の門は、自分が建てられた当の病の実形 2 件に対して 0/2 である。**
> 鳴るのは設計者が書いた教科書どおりの `let` 形だけである。

### 弱い主張 ②【最も危うい】 「偽陽性 0 を実測した」は、**盲点を無罪として成文化したこと**である

なぜ関数の中の形が見えないのか。字句器の (iii) 節に深さの門が在る:

```js
for (const [re, name, why] of NAMELESS) {
  …
  if (dep[g3.index] !== 0) continue;      // ★ 最上位(深さ 0)でなければ罪と呼ばない
```

一方 (ii) の「最上位束縛への破壊的操作」には深さの門が無い —— だから `record.__seen = …` は
**関数の中に書いても捕まる**(私の実測: 🔔 CAUGHT)。
**同じ字句器の中で、名を持つ置き場は深さを問わず罪、名を持たない置き場は最上位でのみ罪。この非対称は文書のどこにも書かれていない。**

そして —— **`gauge(静的): 大域の門は定数を罪と呼ばない` の `trap2` fixture が、その盲点を明示的に凍結している:**

```js
'function f2() { globalThis.__scratch = 1; process.env.X = "1"; }',  // 関数の中(最上位ではない)
…
assert.deepStrictEqual(mutableGlobals([trap2]), [],
  '強めた字句器が偽の赤を出した — 読むだけの globalThis / 関数の中の代入 / …は罪ではない');
```

**これは残債ではない。** 残債は「そこを見ていない」と言う。
この fixture は **「そこには何も無い」と門に宣誓させている。**
第62条(a) が名指した「**言えない盲点は、まだ誰も落ちていない穴**」の、最も悪い形である ——
**盲点が掟の側に成文化された。**

**代は測った。** 深さの門を外して `graph/*.js` の全 22 ファイルを走査した:

```
深さ門を外したときの graph/*.js 全 nameless ヒット = 0
射程 2 本(gauge.js / spawn-trace.js)の現状 = []
[外した後] 関数内 module.exports.x=  🔔 hits=1   関数内 process.env.X=  🔔 hits=1
```

**実樹に偽陽性は一件も出ない。** 直せない理由は無い。直せば `trap2` の 1 行だけが赤くなる ——
**その 1 行こそが直すべきものである。**

### 弱い主張 ③ 「条の元凶 G4 に対する無音率 75% → 0%(6 形すべて CAUGHT)」

PR.md §1 の表の一行である。**6 形はすべて最上位の形である。**
関数の中の形を母集団に入れた瞬間、私の実測で 4 形が無音に戻る。
**「6/6」は正しい数だが、その 6 が何の 6 かを表は語っていない。** これは §3 で裁く第38条の問題と同型である。

### 弱い主張 ④ 「第62条の末文を実体に追いつかせた」

verify の裁定((ii) 実体を条に合わせた)は**筋が通っており、私はこれを支持する**。
外から凍らせる門三本を +73ms で建てたのは立派である。だが凍らせた中身を読むと:

```js
assert.ok(/const STATIC_SCOPE = \[GAUGE_JS, SPAWN_TRACE_JS\];/.test(body), …);
assert.ok(/assert\.strictEqual\(STATIC_SCOPE\.length, 2/.test(body), …);
assert.ok(/mutableGlobals\(STATIC_SCOPE\)/.test(body), …);
```

**凍っているのは射程の「本数」と「名」だけである。** 字句器が**どの深さを見るか**は誰も凍らせていない。
`if (dep[g3.index] !== 0) continue;` の一行を消しても足しても、外の門は一本も鳴らない。
**射程には二つの軸が在る —— どのファイルを見るか(凍っている)と、その中のどこまで見るか(凍っていない)。**
条の末文は前者だけを追いかけた。

### 弱い主張 ⑤ SM-S2 の「今やらない理由」は**測定で偽である**(§4 で裁く)

### 弱い主張 ⑥ 走行帳 `README.md` が **484 門 / 13 本 / engine 8 行**のまま止まっている

```
$ node tests/paradise.test.js --gate-list | tail -1     → Paradise gate list: 492 gates
$ git diff tests/paradise.test.js | grep -c "^+test("   → 21
$ git diff --numstat graph/gauge.js                     → 91  4  graph/gauge.js
```

走行帳の `README.md` は「5 分で分かる版」を自称し、**この走行の総目次**として PR.md §9 が
**読む順の 1 番目**に指定している文書である。そこに書かれている数は
`484` / `13 本` / `engine 8 行` / `SM-N は未払い` —— **すべて verify 相の前の数**である。
追跡ファイルの `README.md` は `census.js fix` が直したが、**走行帳の README は誰の門も見ていない。**
docs 相は追跡ファイルだけを監査し、**自分たちの走行帳を監査しなかった。**
第22条の精神(楽園が自分について語る数はすべて真)が、走行帳には掛かっていない。

### 弱い主張 ⑦ 残債の件数が **10 / 12 / 14 の三通り**ある

| 出所 | 数 |
|---|---:|
| `verify.md` §4 末尾 | 「残債に名を付けた件数: **10**」+「残債にせず直さないと裁定: 2」 |
| 発令書(教主) | **12 件** |
| `PR.md` §6 の表 / 脚注 | **14 件**(SM-S2/S3/L/L2/T/DoS/PATH/AC8/C2/G/H/I/J/M) |

**同じ走行が自分の残債を三通りに数えている。** §4 は 14 件すべてを裁く。

---

### 褒めるべき主張(弱くない)

私は追従しないが、**弱くない主張を弱いと言うのも嘘である。**

- **`verify.md` §3-6b は模範である。** 「security が報告した S-4 の破れを、私は 15 入力で一度も再現できなかった」
  と先に書き、「破れは再現できなかったが、破れうる構造は実在したので塞いだ」と裁定を名乗った。
  **自分に不都合な事実を §3 の頭に置いた。** これは第16条の最良の形である。
- **`findings.md` §5-3 の自己申告。** 実台帳を汚した事故を隠さず、
  「汚さなかったとは書かない。汚し、戻した」と書き、**その事故から第62条(b)と番兵を生んだ。**
- **exit の規約を一文字も動かしていない。** 15 入力 × 改修前後で exit 列が完全一致(verify §3-6b)。
  私も `--audit` の exit が healthy=0 / duplicate=1 / forged-fp=2 のまま動いていないことを確認した。
- **致命 4 件は本当に塞がっている**(§1 の私の実測)。

---

## 3. 数字の誠実さの検査 —— **23.3% → 3.1% は並べてよい数か**

### 裁定: **並べてよくない。そして走行はそれを知りながら、三箇所で並べた。**

| 数 | 母集団 | 撃った者 | 撃った的 | 走らせた門 |
|---|---|---|---|---|
| **23.3%**(14/60) | `gauge.js` **全体**の 9 層 | discover | **engine の実装** | 471 門(絞り込み 168) |
| **3.1%**(2/65) | **新設門の周辺**に自分で発明した変異 | review | **engine + 新設門の本文** | 484 門(絞り込み 129) |

**分母(60 / 65)がたまたま近いことが、この並置を一層たちが悪くしている。**
`60 → 65` は「同じ物差しで少し増やした」ように読める。**違う。的も撃ち手も門も違う。**

**走行はこれを知っている。** 二箇所で正直に書いている:

> `README.md` §11: 「本走行が測った母集団は、discover が測った母集団と**違う**からである。」
> `PR.md` §7: 「**無音率が下がったとは全称で主張しない** —— 母集団が違う(第38条どおり…)」

**知りながら、PR.md §1 の「実測の表(前 / 後)」の第 2 行に並べた。**

```
| **無音率**(変異を撃って一本も鳴らない率) | **23.3%**(60 変異中 14) | **3.1% → 0%** |
```

この表の題は「**前 / 後**」である。**前後の表に置かれた数は、同じ物差しの前後だと読まれる。**
§7 の但し書きは 280 行下に在る。**PR を 5 分で読む者は §1 の表しか見ない。**
PR.md §9 は「**急いでいる人は 1 → 2 → 4 だけでよい**」と自ら案内している —— **§7 は読まれない設計である。**

**誰読を生むか**: 「楽園は無音率を 23.3% から 3.1% に、さらに 0% に下げた」。
そして次の走行は **0% を基線として** 建てる。第62条自身の言葉を借りれば:

> **掟が実体より大きく名乗れば、次の走行はその嘘を土台にする。**

**数についても同じである。** verify 相は第62条の末文について「実体を条に合わせる」を選んだ。
**同じ物差しを §1 の表に当てれば、この行は消すか、二行に割って母集団を各々に書くかの二択である。**

### 私が正しいと考える書き方

```
| 無音率(discover の物差し: gauge.js 全層 × 60 変異 × 471 門)      | 23.3% | 次の変異走行が測り直す(未測) |
| 無音率(review の物差し: 新設門周辺 × 65 変異 × 484 門)           | —     | 3.1% → verify 後 0% |
```

**「前」の欄を空にできないなら、その行は前後の表に置いてはならない。** それが第38条である。

### 数の誠実さについて、もう一つ

`PR.md` §1 の「**門の総数 471 → 492**」と「**engine の実コード 19 行**」は**私が実測で確かめた**
(`--gate-list` = 492 / `+test(` = 21 / `gauge.js` = +91-4)。**この二つは真である。**
問題は無音率の一行だけであり、**表全体が嘘なのではない。**

---

## 4. 残債 14 件の裁定 —— **「今やらない理由」か、それとも面倒だっただけか**

**基準**: 理由が (1) 実測か条文に立ち、(2) 払う条件が数か手順で書かれ、(3) 今日直す代が測られている —— なら**正当**。
「構造を動かすから」だけで、**動かさずに済む道を測っていない**なら**面倒**。

| 残債 | 裁定 | 根拠 |
|---|:---:|---|
| **SM-S2** 走行を通して台帳を隠せば番兵は skip のまま | ❌ **面倒** | **理由が測定で偽である。§4-1 に実測を載せる。** |
| **SM-L2** 字句器がなお 4 形を見ていない | ❌ **面倒** | **名指しが不完全で、最大の穴が名を持たない。§4-2 に実測を載せる。** |
| **SM-I** `conclave.json` 以外の `.json` を拾う退行に門が無い | ❌ **面倒** | 理由は「拾ってよい名の集合が二箇所に住む(第48条)」。だが実体は `for (const name of ['conclave.json'])` の**一行**であり、定数に括り出す代は 1 行である。**その 1 行の代を誰も測っていない。** 私の実測では今日も **SILENT**。findings は B8 を【重大】と呼んでいる |
| **SM-C2** 既存の決定性門にコメント一行 | ❌ **面倒** | 理由は「NFR-2(既存 471 門を一本も触らない)」。**NFR-2 は本走行が自分に課した都合であって掟ではない。** 註釈一行のために自作の制約を盾にしている。代は 0 秒 |
| **SM-S3** 番兵が外部の追記と門の汚染を分けられない | ✅ **正当** | 形(`appended`/`rewritten`/`truncated`)まで分けた上で、**分けられない部分を門の文面に書いた**。「門ごとに指紋を採り直す」修理が**見落としに化けうる**(最初の一本だけ鳴り後続は緑)という対価を名指し、「この裁定には測定が要る」と述べている。**測らずに直さないのは第38条の遵守である** |
| **SM-L** 静的門の射程外に既知の可変大域 3 ファイル 6 件 | ✅ **正当** | 第62条(c) が命じたとおり**中身に名を与え**(`pulse.js:_gateCache` / `hermetic.js:TRACKED` / `abode.js:unmeasurable`)、門が毎回画面で名乗り、外から凍らせる門がその名乗りを assert する。**残債の模範形である** |
| **SM-G** 時刻の物差しが二箇所に散っている | ✅ **正当**(ただし三度目の持ち越し) | リファクタと門を同じ PR に混ぜない(第57条)は正しい。私の実測でも T7 は今日 **SILENT**。**だが requirements から数えて三走行目の持ち越しであり、次は払え** |
| **SM-H** `baseline` が junction を辿らないことに門が無い | ✅ **正当** | CI(ubuntu)に `mklink /J` が無く、素直に書けば恒常 skip の門になる = 第37条を自分で破る。跨プラットフォーム設計が要るのは本当である。私の実測でも B7 は **SILENT** |
| **SM-T** AC-13 の子プロセスにタイムアウトが無い | ✅ **正当** | 「値を推測で置くのは第16条違反」「払う条件: CI で 10 回測り中央値の 5 倍」—— **数で払う条件が書かれている。模範形** |
| **SM-DoS** 番兵の代が台帳の大きさに線形 | ✅ **正当** | 今日の代 2.6ms を実測し、「実害 0 なので測る前に直さない」。第38条の正しい適用 |
| **SM-PATH** 走行記録にホームパス 21 箇所 | ✅ **正当** | 追跡ファイルの差分には無く、走行記録は実測の生出力。**公開の可否は教主の権能**であり、神官が勝手に消さないのは正しい |
| **SM-AC8** AC-8 の CLI 経路を撃っていない | ✅ **正当** | 既存門が同じ規約を同じ道で撃っており、足せば +150ms で二度撃つだけ(第44条の冗長)。「**正しい払い方は AC の文面を実体に合わせること**」は正論 |
| **SM-J** `gauge-audit.test.js` に番兵が無い | ✅ **正当** | 当該ファイルの実台帳枝は読むだけ。器が二箇所に住むのを避ける(第48条) |
| **SM-M** `:6060` の門の改名 | ✅ **正当** | **門の名は `--gate` の契約面**である。名を動かす変更を同じ PR に混ぜないのは第57条どおり |

**裁定: 正当 10 / 面倒 4。**

### 4-1. SM-S2 —— 「外から区別する道が今日は無い」は**測定で偽である**

`verify.md` §4 の理由:

> 本物の不在と偽装の不在を**外から区別する道が今日は無い**。`workspace.js` の `source` を見る案は
> **住所解決の意味を変える改修**であり…
> **払う条件**: `workspace.js` が「倉は在るがファイルだけ無い」を名乗る**口を持つ単独 PR**。

**その口は既に在る。** 私は三通りの機を作って `resolve()` を撃った:

```
[A: 倉あり・台帳あり = 神の機]     {"source":"sibling","exists":true, "vault":…}
[B: 倉あり・台帳だけ隠した = S-2]  {"source":"sibling","exists":true, "vault":…}   ← 倉は在る
[C: 倉ごと無い = CI の機]          {"source":"default", "exists":false,"vault":null} ← 倉が無い
```

**B と C は `source` と `exists` の両方で既に分かれている。** `resolve()` の返り値の型宣言
(`graph/workspace.js:51`)にも `exists:boolean` と `source:'env'|'sibling'|'legacy'|'default'` が載っている。
**番兵が「`exists===true` なのに台帳ファイルだけ無いなら skip しない」と書くのに、`workspace.js` を一文字も変える必要はない。**
住所解決の意味は動かない —— **既に在る欄を読むだけである。**

**ゆえに SM-S2 の「今やらない理由」は成立しない。** そして SM-S2 は
**security が【致命】と呼んだ S-2 の残り半分**である。致命の半分を、偽の理由で次に送った。

> **公平に書く**: 私は「その 3 行を書けば直る」と実測したのではない。**測ったのは「口が既に在る」ことだけ**である。
> 書いた後に CI(倉が無い機)で偽の赤が出ないかは**私も撃っていない**(§7)。
> だが「**道が無い**」と「**道は在るが通ってみていない**」は別の言葉であり、**残債に書くべきは後者である。**

### 4-2. SM-L2 —— 名指しが不完全で、**最大の穴が名を持たない**

SM-L2 が名指す 4 形: 動的 require / `Object.assign` / `eval` / Proxy。
**私の実測で無音だった 4 形のうち、この表に載っているのは `Object.assign` の 1 形だけである:**

| 無音だった形 | SM-L2 に名が在るか |
|---|:---:|
| 関数内の `globalThis.__m ||= new Map()` | ❌ **無い** |
| 関数内の `module.exports.__stash = {}` | ❌ **無い** |
| 最上位の `Object.assign(globalThis, {…})` | ✅ 在る |
| 関数内の `Reflect.set(globalThis, …)` | ❌ **無い** |

しかも根本原因 —— **「名を持たない置き場は深さ 0 でのみ罪と呼ぶ」という字句器の非対称** ——
は走行帳のどこにも一行も書かれていない。第62条(c) は
「**射程は名指しで狭く切り、射程の外は次の走行の残債として名を持て**」と命じている。
**名を持たない射程外は条文違反である。**

そして §2 の弱い主張 ② で示したとおり、**`trap2` fixture が最大の穴を「罪ではない」と凍結している。**
**これは残債ではなく欠陥である。** 残債は「見ていない」と言う。この fixture は「**無い**」と宣誓する。

---

## 5. 過去の教訓との照合

```
$ node graph/lessons.js export --out $LOCALAPPDATA/Temp/lessons.json
$ node graph/critic.js review graph --lessons $LOCALAPPDATA/Temp/lessons.json --self
VERDICT: the critic found nothing (107 件の教訓で裁いた). Proceed to judgment.     EXIT=0

$ node graph/critic.js review reform/silent-mutations --lessons $LOCALAPPDATA/Temp/lessons.json
VERDICT: 3 GAP(S) — the creation is incomplete. REWORK.                            EXIT=1
```

**engine 側(`graph`)は 107 件の教訓すべてを通過した。** 走行帳側の 3 件を一つずつ裁く。

### 🔴 1. `tests-exist`「この改修は tests/ に一本も門を書いていない」 → **誤射。走行の罪ではない。**

`critic` は `reform/silent-mutations/` を見ており、そこには散文しか無い。
教訓 `reform-lives-in-three-places` が名指したとおり
**reform は散文 `reform/<slug>/`・実装 `graph/`・門 `tests/` の三箇所に住む**(第23条)。
実体は `git diff tests/paradise.test.js` に **`+test(` 21 本 / +1301 行**。
**この赤は critic の射程の問題であって、走行の欠陥ではない。**

### 🔴 2. `final-clause-carries-the-ask` → **誤射。** forge の熟議判定の教訓であり、本走行とは層が違う。

### 🔴 3. `corpus-blind-to-its-own-half` → **本物である。そして §2 の私の実測と正確に一致する。**

> 「門のコーパスが規則の片枝しか含まないなら、門は永久に緑のまま面の半分を素通しする。
> **故障注入の変異も同じ —— 全変異が片枝を狙うなら、他枝について何も証明していない**」

**本走行の静的の門は、まさにこの形で片枝しか持っていない。**

| 規則の枝 | コーパス | 深さの扱い |
|---|---|---|
| **名を持つ最上位束縛**(`let X` / `X.y =`) | 在る(`bad.js` / `trap.js`) | **深さを問わず罪**(私の実測: 関数内でも CAUGHT) |
| **名を持たない置き場**(`globalThis` 等) | 在る(`bad2.js` の 5 形) | **深さ 0 のみ罪。関数内は `trap2` が無罪と凍結** |

**「6/6 CAUGHT」は片枝の 6/6 である。** 教訓の言葉どおり、**他枝について何も証明していない。**
`critic` はこの走行を読んで、この教訓が未対処だと機械で判じた。**機械の方が正しい。**

### 「修理が次の病を生む」系の教訓との照合(発令書が特に問えと命じた件)

#### `repair-opens-a-new-surface`(CONDUCT)
> 「修理は次の攻撃面を開く —— 差分が塞いだ穴でなく、**差分が新しく可能にしたこと**を撃て」

**本走行はこの教訓を最もよく守った走行である。** security 相が §2-6 を丸ごと
「**第62条が開けた窓**」に充て、A47 で「条文が**門が現物に触れる**を恒久的に祝福した」と摘発した。
番兵という新しい攻撃面を自分で名指し、そこに 10 本以上の攻撃を撃った(A1〜A10 / A47〜A49)。
**教訓は守られている。**

**ただし、教訓の言葉を一段深く当てると破れている**:
**静的の門もまた「差分が新しく可能にしたこと」である。**
security は番兵を攻撃面として撃ったが、**静的の門は「騙せるか」の軸(A29-A45)でしか撃たれていない。**
「**この門が無罪と宣言する形は何か**」という軸で撃った者は一人もいない。
そして無罪の宣言(`trap2`)の中に、致命の実形が住んでいた。
**修理が開けた面は「攻撃されうる面」だけではない。「免除される面」も面である。**

#### `mutation-author-is-blind-to-own-net`(CONDUCT)
> 「変異を設計した者と門を設計した者が同じなら、変異は門の想像の外を撃たない。
> **網の証明は必ず別の者が別の軸で撃つまで保留せよ**」

**本走行はこの教訓の生きた証拠を三重に積んだ**: prove が「25/25 無音ゼロ」→ review が別軸で SILENT 2 →
security が別軸で致命 3。**そして今 —— 私が四重目を積んだ。**
verify が「6/6 CAUGHT / 偽陽性 0」と名乗った門に、私が別の軸(**深さ**)で撃って 4 形が無音だった。

**四度連続で、同じ形で、同じ走行が裏切られている。**
教訓は「保留せよ」と命じている。**PR.md §1 の表はその保留をしていない。**

#### `conduct-lesson-presented-is-not-enforced`
> 「規範の教訓は**提示だけでは守られない**。…その教訓が名指す行為を**発令書の必須項目へ降ろせ**」

**本走行でもこれが再発した。** `repair-opens-a-new-surface` と
`mutation-author-is-blind-to-own-net` は critic が毎回 ✓ CONDUCT として提示している。
提示されたうえで、**新設門に対する網羅的な故障注入を誰もやっていない**:

- security §4-5 U-23:「**11 本の新設門は『壊したら鳴るか』を私が確かめていない**」
- verify §7-6 16:「**新設 8 門の assert を一本ずつ殺す網羅は撃っていない**」

**両者が「やっていない」と正直に書き、両者ともやらなかった。** 提示は振る舞いを変えなかった。

---

## 6. 次の走行が撃つべき層

findings §5-2 は「**新しい層を選べばまた出る**」と書いた。**この予言は三度当たった**
(review が別層で SILENT 2 / security が別層で致命 3 / 私が別層で無音 4)。
**予言が三度当たったなら、次も当たる。** 名指す。

### 一番手 —— **「門が無罪と宣言した形」の層**(免除・境界 fixture の層)

**未踏である。** 誰も撃っていない。

これまで全員が「**門を騙せるか**」を撃ってきた。
**「門が自分から『これは罪ではない』と宣誓した形の中に、罪が住んでいないか**」を撃った者は一人もいない。
私が一つ撃っただけで当たった(`trap2` の `function f2() { globalThis.__scratch = 1; }`)。

**撃ち方**: `tests/paradise.test.js` の全門から `deepStrictEqual(…, [])` /
`assert.ok(!…)` / `=== 0` / `skip` を名乗る枝を機械で列挙し、
**各々の「無罪の fixture」に本物の病を一件ずつ植えて走らせる。**
偽陽性の境界を測る門は楽園に多数在る —— **そのすべてが同じ病を持ちうる。**

**なぜ一番手か**: これは**門の書き方の層の一段下**である。
findings が 40% で最悪と名指した層を本走行が閉じにいき、
**閉じるために書いた免除がそのまま新しい層になった。** 層は下がっただけで消えていない。

### 二番手 —— **`baseline` 走査の層(倉の外 / 拾う名)**

findings が 38% と測り、**21 門を建てた後も私の実測で今日なお無音**:

```
B7(junction を辿る)     ❌ SILENT exit=0 red=0    ← findings 分類【重大】/ 残債 SM-H
B8(.json を全部拾う)     ❌ SILENT exit=0 red=0    ← findings 分類【重大】/ 残債 SM-I
```

**第30条(倉の外に触れない)を junction 一本で破れる道が、名を持ったまま開いている。**
SM-H の跨プラットフォーム設計(`fs.symlinkSync(target, link, 'junction'|'dir')` の分岐)は
**design 相の仕事**であり、単独の走行に値する。

### 三番手 —— **「二つの口が同じ物差しを使うか」の層**

```
T7(audit の未来判定だけ 365 倍)  ❌ SILENT exit=0 red=0    ← 残債 SM-G / 三走行目の持ち越し
```

`record` と `--audit` が時刻について**別々の式**を持っている。第48条(同じ問いに二つの答えを持つな)は
本走行が `healable` について適用した掟である —— **同じ掟を時刻に適用していない。**
**`healable` を直した走行が時刻を直さなかったのは、一貫していない。**

### 撃ってはならない層(次の走行への戒め)

**`exit` と `並行性` をもう一度撃つな。** findings の実測で無音 0% であり、
本走行が更に厚くした。**そこを撃てば無音率は綺麗に下がるが、地形は一ミリも変わらない。**
findings §3-2 が自分で書いている ——「**下がった理由の半分は層の選び方である**」。

---

## 7. 私が見ていないこと(第16条)

**この節が本報告で最も重要である。** §2 の私の摘発は、以下の限界の上に立っている。

1. **私は全走(492 門 / 6 分)を一度も走らせていない。** 撃ったのは
   `--gate gauge --gate 静的 --gate 番兵 --gate 並行 --gate 門ヘルパー`(137 of 492)の
   絞り込み走行である。**出力自身が「絞り込み走行は門の依存を保証しない」と警告している。**
   **無音と判定した 7 件のうち何件が残り 355 門で鳴るかを、私は知らない。**
   (security も U-25 で同じ限界を名乗っている —— **私はその限界を引き継いだだけで、越えていない。**)
2. **私が撃った変異は 15 件である。** discover の 60、review の 65、security の 55 に比べて桁が少ない。
   **私は「層を選んで少数を撃つ」戦術を採った** —— 層の選び方が当たったので当たったのであって、
   **網羅ではない。私が選ばなかった層について私は何も言えない。**
3. **無音率という数を私は出さない。** 私の 15 件は**盲点を狙って選んだ**偏った標本であり、
   これを率にすれば §3 で私が裁いたのと同じ罪を犯す。**`7/15 = 47%` という数を書いてはならない。**
4. **深さの門を外す修理を、私は門の上で走らせていない。** 測ったのは
   「`graph/*.js` 22 ファイルで新たな当たり 0 件」と「`trap2` が赤くなる」までである。
   **492 門を実際に走らせて他に赤が出ないことは確かめていない。** 修理の代を私は数で持っていない。
5. **SM-S2 について、私は「口が在る」を測っただけである**(§4-1 に自分で書いた)。
   **その口を使った番兵が CI で偽の赤を出さないことは撃っていない。**
6. **CI(ubuntu)で一度も撃っていない。** verify も security も同じ限界を名乗った。
   **三人が同じ場所で止まっている。** 特に `RACE_LEAD_MS = 400` が CI で足りるかは**誰も測っていない。**
7. **私は `tests/paradise.test.js` の +1301 行のうち、読んだのは約 250 行である。**
   静的の門・番兵・並行の門の本文と、`mutableGlobals` の全文。
   **残り 21 本のうち 15 本は一行も読んでいない。そこに穴があっても私は見つけられない。**
8. **`design.md`(1335 行)と `build.md` / `prove.md` / `docs.md` を、私は目次と要約でしか読んでいない。**
   §Z(決めきらなかったこと 7 件)の中身を一件ずつ裁いていない。
9. **第62条の条文 58 行を、私は一度も変異で撃っていない。** 読んだだけである。
   `codex.js check` が exit 0 であることは教主の実測を引き継いだ。
10. **私が「褒めるべき」と書いた致命 4 件の封鎖も、各々一形しか撃っていない。**
    S1/S4/T9/G8 の**等価形**(別の書き方で同じ病を起こす形)を撃てば、
    §2 で静的の門に起きたのと同じことが起きるかもしれない。**私はそれを撃っていない。**
11. **私は verdict を出していない。** 出すのは creation-judge であり、私は勧告するだけである。

---

## 8. verdict 相への勧告

### 推す判定: **REWORK**(狭い。一巡。修理の当たりは実測で添える)

**これは走行の質の否定ではない。** 致命 4 件は本当に塞がり(私の実測)、
492 門が三本とも 0 failed で、exit の規約は一文字も動かず、実台帳は不変で、
`verify.md` §3-6b は楽園が書いた最も正直な節の一つである。**この走行は強い。**

**それでも REWORK を推すのは、本走行自身が同じ場面で下した裁定に従うからである。**

第62条の末文が実体より大きく名乗ったとき、verify 相は二択を置いた:
**(i) 条を実体に合わせて弱める / (ii) 実体を条に合わせて建てる。**
verify は **(ii)** を選び、その根拠をこう書いた:

> **掟が実体より大きく名乗れば、次の走行はその嘘を土台にする** —— ならば実体を追いつかせるのが筋である。

**いま同じ形の食い違いが三つ在る。**

| # | 名乗り | 実体(私の実測) |
|---|---|---|
| **R-1** | 「G4 の 6 形すべて CAUGHT / **偽陽性 0**」 | 関数内の `globalThis` / `module.exports` / `Reflect.set` / 最上位 `Object.assign` の **4 形が無音**。致命の実形 G8/G6 に対し静的の門は **0/2**。しかも `trap2` が危険な形を**無罪として凍結**している |
| **R-2** | 「無音率 **23.3% → 3.1% → 0%**」(PR.md §1 前/後の表) | 母集団・的・撃ち手・門がすべて違う。**走行自身が §7 でそう書いている** |
| **R-3** | 残債 **SM-S2**「外から区別する道が今日は無い」 | `workspace.resolve()` が **`exists` と `source` で既に区別している**(実測 3 通り)。致命 S-2 の残り半分を偽の理由で送った |

**三件とも「実体より大きい名乗り」であり、三件とも本走行が他人に対して適用した物差しで裁ける。**
**自分の物差しで自分を裁かないなら、その物差しは物差しではない。**

### REWORK の射程(これ以上は求めない)

1. **R-1**: 字句器の深さの非対称を解く(`if (dep[g3.index] !== 0) continue;`)か、
   **解かないなら `trap2` から関数内 `globalThis` 代入の行を外し、深さの制限を残債に名指しで書く。**
   —— **代は測ってある**: 深さの門を外しても `graph/*.js` 全 22 ファイルで**新たな当たりは 0 件**。
   実樹に偽陽性は出ない。**どちらを選ぶかは構築の権能であり、私は選ばない。**
   併せて「6/6 CAUGHT」の 6 が**最上位形の 6** であることを名乗ること。
2. **R-2**: PR.md §1 の無音率の行を**二行に割る**(各々に母集団を書く)か、**前後の表から外す**。
   §7 の但し書きは残す。**PR を 5 分で読む者に嘘が届かないようにすること。**
3. **R-3**: SM-S2 の「今やらない理由」を**測定に合わせて書き直す**。
   「道が無い」ではなく「**口は `workspace.resolve().exists`/`source` に既に在るが、
   それを読む番兵が CI で偽の赤を出さないことを撃っていない**」。理由が変われば払う条件も変わる。
4. **走行帳 `README.md`** の 484 / 13 本 / engine 8 行 / SM-N 未払い を実体(492 / 21 本 / 91 行 / SM-N 払済)に直す。
   **PR.md が読む順の 1 番目に指定している文書である。**
5. **SM-L2 の名指しを補う** —— 関数内 `globalThis` / 関数内 `module.exports` / `Reflect.set` /
   **深さの非対称そのもの**を書く。第62条(c) は「射程の外は名を持て」と命じている。

**求めないこと**: 新しい門を建て直すこと。B7/B8/T7 を今払うこと(SM-H/SM-I/SM-G は正当な残債である)。
engine を動かすこと。**5 件のうち 4 件は散文の修正であり、engine には一行も触れない。**

### なぜ BLOCK でないか

- **実害が一件も無い。** G8 / G6 / G7 の**有害な形はすべて赤くなる**(私の実測で red=5 / red=10)——
  鳴るのが静的の門ではなく既存の振る舞いの門だというだけである。**退行が今日すり抜ける道は測定で存在しない。**
- **実台帳は不変**(私の 15 回の走行の各回で照合)。**exit の規約は不動。** 作業木は 5 ファイル + 走行帳のみ。
- **492 passed / 0 failed が三本**、CI 相当 12 本すべて exit 0(教主の実測)。
- **致命 4 件は本当に塞がっている**(私が自分で撃ち直した)。

### なぜ SHIP でないか

- **R-1 は残債ではなく、門の中に成文化された盲点である。** 残債は「見ていない」と言う。
  `trap2` は「**無い**」と門に宣誓させる。**第62条(a) が最も恐れた形である。**
- **R-2 は PR を読む者が最初に見る表に在る。** §7 の但し書きは 280 行下で、
  PR.md 自身が「急いでいる人は §7 を読まなくてよい」と案内している。
- **`critic` が機械で REWORK を出しており、その 3 件のうち 1 件(`corpus-blind-to-its-own-half`)は
  私の独立の実測と正確に一致した。** 機械と人が別々の道から同じ穴に着いた。
- **代が安い。** 5 件のうち 4 件は散文。engine は一行も動かない。**一巡で閉じる。**

### 領域の批准について(私は打たない)

`tribunal` は `reflect:pending / verdict:pending` で **5/6** である。
**批准は教主の権能であり、私は `conclave.js ratify` を打っていない。**
ただし教訓 `ring-not-closed-by-merge` と、skill が名指す
「**judge が verdict を先に書くと `domainsRatified` が足りず点が 20 点低く出て SHIP が REWORK に化ける**」
という落とし穴を、verdict 相は先に読むこと。**先に ratify し、それから `gauge.js score --json` を撃ち直せ**(第27条)。

---

## 9. 結び

**この走行は、自分について語ったことの大半が本当だった。**
致命 4 件は塞がり、exit は動かず、実台帳は不変で、
`verify.md` §3-6b と `findings.md` §5-3 は楽園が書いた最も正直な二つの節である。

**本当でなかったのは三つ、すべて同じ形をしている ——「実体より大きい名乗り」。**

そして皮肉なことに、**その三つは本走行が成文した第62条が名指した形そのものである。**

> **(a) 門を建てるとき、その門の形が何を見えなくするかを言え。**
> **言えない盲点は、存在しない盲点ではなく、まだ誰も落ちていない穴である。**

静的の門は自分の形が何を見えなくするかを言わなかった。**深さを言わなかった。**
そして言わないどころか、**見えない形を「罪ではない」と fixture に凍結した。**

条を書いた者が条の病にかかる —— `README.md` §11 は、それを**この走行の誇るべき発見**として書いた。
**その発見は、書いた後にもう一度起きた。**

**一巡で閉じる。それだけである。**

---

*実測に使った複製と変異器: `$LOCALAPPDATA/Temp/sc/` / `$LOCALAPPDATA/Temp/sc-reflect/`(揮発)。*
*変異走行 15 回。各回で `restored=true` と実台帳 sha256 の一致を印字した。*
*実台帳 `955ea34a020bf5681b5607de9ed22abaddc7bc5228cb231dd843c05ba55aab77` / 7 行 —— 本相の前後で不変。*
*`graph/*.js` と `tests/*.js` に一行も書いていない。commit も push もしていない。*
