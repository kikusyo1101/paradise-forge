# verdict — reform 走行 `silent-mutations` の最終裁定

**裁く者**: creation-judge(独立した断罪機関 / 第11条 —— どの枢機卿にも属さない)
**日付**: 2026-09-17
**姿勢**: **前相が REWORK を出したことは、私が REWORK を重ねる理由にならない。実測で裁く。**
reflect の告発も rework の弁明も**自分の手で撃ち直した**。走行帳から写した数は一つも無い。

```
[開始] $ sha256sum ../paradise-creations/gauge-ledger.jsonl
955ea34a020bf5681b5607de9ed22abaddc7bc5228cb231dd843c05ba55aab77   7 行
[全実測の後]
955ea34a020bf5681b5607de9ed22abaddc7bc5228cb231dd843c05ba55aab77   7 行
```

**変異はすべて複製に撃った。`git checkout -- <file>` は一度も使っていない。**
生バイト列を保存 → 書き戻し → **sha256 一致**で復元を毎回証明した。
**`graph/*.js` と `tests/*.js` に一行も書いていない。commit も push もしていない。**

---

# 裁定: **SHIP**

```
$ node graph/verdict.js judge reform/silent-mutations/verdict-report.json
✅  SHIP — All gates pass, no breach — creation is complete.
  ✓ trajectory 80/100   ✓ build passes   ✓ 492/492 tests pass
  ✓ no security issues  ✓ spec satisfied
EXIT=0
```

### 根拠の三行

1. **rework の五件はすべて実測で真だった。** 四形 4/4 CAUGHT / 偽陽性 6 件が既知のまま増減 0 /
   `trap2` の盲点は消え逆側の凍結が退行四形を捕まえる —— **私が自分で撃って確かめた。**
2. **全相が揃って見ていなかった層を、私が撃って閉じた。** rework 自身が「最も重い未測」と名指した
   **CI(ubuntu 形)で偽の赤が出るか**を本物の `git clone` 二本で測り、**本走行 490/0/2skip exit 0 ・
   baseline 469/0/2skip exit 0・差は正確に +21 門**。**偽の赤は出ない。**
3. **実害のある欠陥が一件も無い。** 新たに見つけた三件はすべて **CI と神の機の両方で exit 0 のまま**であり、
   名と理由と払う条件を持つ残債(SM-V1/V2/V3)に落とせる。**一つの PR で閉じられる。**

---

## 1. rework の主張の検算 —— **五件とも真だった**

### 1-1. 四形は本当に鳴るか → **鳴る。4/4。**

複製 `$LOCALAPPDATA/Temp/vj/paradise` の **`graph/gauge.js` の現物**に四形を撃ち込み、
`--gate gauge --gate 静的 --gate 番兵`(131 of 492 門)を走らせた。

```
[gauge.js sha256 前] ea4e4e1bfbf1abe358a293f188486f0d4c5d1c4211424231fa7db6b464a30d2b

🔔 CAUGHT | 形1 関数内 globalThis.__seen =  (G8 の実形)   | exit=1 red=1 restored=true
            131 of 492 gates matched — 129 green, 1 red, 1 skipped
            ✗ gauge(静的): 台帳を書く engine は最上位に可変の大域を持たない (D-L / 第62条 c)
🔔 CAUGHT | 形2 関数内 module.exports.__stash =           | exit=1 red=1 restored=true
🔔 CAUGHT | 形3 関数内 Reflect.set(globalThis,…)          | exit=1 red=1 restored=true
🔔 CAUGHT | 形4 最上位 Object.assign(globalThis,…)        | exit=1 red=1 restored=true
   CTRL   | 対照(無変異)                                  | exit=0 red=0 restored=true
            131 of 492 gates matched — 130 green, 0 red, 1 skipped

[gauge.js sha256 後] ea4e4e1bfbf1abe358a293f188486f0d4c5d1c4211424231fa7db6b464a30d2b  restored=true
SILENT = 0 / 4
```

**reflect が「全 492 門に無音」と測った四形は、いま四形とも鳴る。**
対照(無変異)が exit=0 であることが、赤が変異由来であることを保証している。

### 1-2. 偽陽性は本当に増えていないか → **増えていない。当たりは既知の 6 件だけ。**

`tests/paradise.test.js:3810-4018` から字句器を **現物のまま切り出し**(一文字も改変せず)、
`graph/*.js` **39 ファイル**に撃った。

```
静的の門の深さの射程: 深さを問わない 7 形 [globalThis / module.exports / process.env /
  require.cache / Object.assign(globalThis) / Reflect.set(globalThis) /
  Object.defineProperty(globalThis)] / 深さ 0 のみ 1 形 [(分割代入)]
        └ なぜ (分割代入) だけ深さで切るか: `let {a} = …` は束縛の宣言であり…

[射程] graph/*.js = 39 ファイル(全走査)
全当たり = 6 件 / 3 ファイル
  abode.js  (1)     :1506  unmeasurable  最上位束縛への破壊的操作
  hermetic.js  (3)  :516/:519/:522  TRACKED
  pulse.js  (2)     :170/:203  _gateCache

既知 3 ファイル(SM-L = pulse/hermetic/abode)の外の当たり = 0 件 []
射程 2 本(gauge.js / spawn-trace.js)の当たり = 0 件 []
```

**rework の主張(6 / 0 / 0)と完全一致。** reflect が「22 ファイル」と書いた数は
**実測では 39 である** —— rework の訂正の方が正しい。

### 1-3. `trap2` から盲点の成文化が消えたか → **消えた。そして逆側の凍結が効いている。**

発令書が命じたとおり、**`topOnly:false` を `true` に戻す変異**で逆側の凍結を撃った。
複製 `vj2/paradise` の `tests/paradise.test.js` に対して。

```
[sha256 前] d5eb82b6bdb92a6135968f26f763e4176a17417c9ca9ed60cfa27616b41af86e

🔔 CAUGHT | M-A 全形 topOnly:false → true(旧の非対称を表ごと復活)      | exit=1 red=3
            ✗ gauge(静的): 台帳を書く engine は最上位に可変の大域を持たない
            ✗ gauge(静的): 大域の門は定数を罪と呼ばない — 偽の赤を出さない境界
            ✗ gauge(静的): 静的の門の射程は外から凍らされている
🔔 CAUGHT | M-B 実装だけ旧に戻す(if (form.topOnly && d!==0) → if (d!==0)) | exit=1 red=3
🔔 CAUGHT | M-C globalThis 一形だけ topOnly:true(部分退行)              | exit=1 red=3
🔔 CAUGHT | M-E namelessDepthPolicy() の呼出を消す(名乗りの死)          | exit=1 red=1

[sha256 後] d5eb82b6bdb92a6135968f26f763e4176a17417c9ca9ed60cfa27616b41af86e  restored=true
```

**退行の四形すべてが鳴る。** しかも `M-C`(**一形だけ**を戻す部分退行)まで捕まる ——
`bad3` が五つの当たりを名前つきで数えているからである。
`trap2` の当該行(`function f2() { globalThis.__scratch = 1; … }`)は実物から消えており、
**同じ形が `bad3` で罪として凍結されている**(`deep.length === 5` / 全て `depth > 0` / 全て「深さ N」を名乗る)。

> **reflect が「門の中に成文化された盲点」と呼んだものは、実体として消えている。**
> そして「消えたことを凍らせる門」も立っている。**告発は閉じた。**

### 1-4. 残債 14 件の正本化 → **PR.md §6 と README.md §9 が完全一致**

```
$ diff <(PR.md §6 の SM-*) <(README.md §9 の SM-*)   → SAME(14 件 / 14 件)
SM-AC8 SM-C2 SM-DoS SM-G SM-H SM-I SM-J SM-L SM-L3 SM-M SM-PATH SM-S2 SM-S3 SM-T
```

reflect が摘発した 10/12/14 の三通りは正本化された。

### 1-5. SM-S2 を実際に塞いだか → **塞がっている。そして `hidden` の判定は実機で正しく動く。**

`ledgerAbsenceKind()` を現物から切り出し、**三通りの機を実際に作って**撃った。

```
[CI の機: 兄弟倉ごと無い]       resolve → {"source":"default","exists":false,"vault":null}
[S-2 の機: 倉は在るが台帳なし]  resolve → {"source":"sibling","exists":true, "vault":false}
                                 ledgerAbsenceKind() → kind="hidden"
```

**`exists` で本物の不在と偽装の不在が分かれる**という rework の主張は真である。
そして `hidden` は実機で実際に返る —— 純関数の assert だけではない。

---

## 2. まだ誰も見ていなかった層 —— **私が撃った**

各相の「私が見ていないこと」を突き合わせると、**四人全員が同じ場所で止まっていた**。

| 相 | 自白 |
|---|---|
| verify §7 | CI(ubuntu)で撃っていない |
| security U-25 | CI で撃っていない / 新設門を壊して鳴るか確かめていない |
| reflect §7-6 | CI で撃っていない —— 「**三人が同じ場所で止まっている**」 |
| rework §5-6 | CI で撃っていない —— **四人目**。「**R-4 の修理が CI で偽の赤を出さないか。これが本相の最も重い未測項目である**」 |

**五人目にはならない。撃った。**

### 2-1.【最重要】CI(ubuntu 相当)の形で全走した —— **偽の赤は出ない**

発令書は「これは PR を出せば CI が答える」と書いた。**答えを待たずに測れる。**
本物の `git clone` を二本作った(`.git` を持つ。私の最初の tar 複製は `.git` が無く
`hermetic` 系 3 門が偽の赤を出した —— **その 3 件は私の複製の欠陥であって走行の欠陥ではない**)。

| 機 | 中身 | 結果 |
|---|---|---|
| **本走行** | `git clone` + 未 commit の 5 ファイル + 走行帳。兄弟倉 **無し** | `490 passed, **0 failed**, 2 skipped` **exit 0** |
| **baseline** | `git clone`(HEAD 素 / 本走行の差分なし)。兄弟倉 **無し** | `469 passed, **0 failed**, 2 skipped` **exit 0** |

```
· skip: 実台帳が無い(…\vj5\ci\paradise-creations\gauge-ledger.jsonl)—
  不在の形=absent: 兄弟倉そのものが無い(…)— 第37条が認めた不在の機(CI がこれ)。
```

**門数は 471 → 492(`--gate-list` 実測)、gauge 節は 110 → 131 —— 差は正確に +21。**
CI の他の門も兄弟倉なしで撃った: `workspace check` / `conclave audit` / `hermetic check` /
`derived check` / `codex check` / `wiring check` / `check-agents` / `clergy lexicon-check`
**すべて exit 0。しかもどれも兄弟倉を作らない。**

> **rework が「本相の最も重い未測項目」と名指した件は、解消された。**
> **R-4 の修理は CI を赤にしない。** これは裁定を **REWORK → SHIP** に動かす実測である。

### 2-2.【新発見 SM-V3】兄弟倉が「在るが空」の機では 58 門が赤くなる

rework が恐れた形(「もし CI の機で兄弟倉ディレクトリだけが存在すれば」)を**実際に作った**。

| 機 | 本走行 | baseline(HEAD 素) |
|---|---|---|
| CI 形 + `mkdir ../paradise-creations`(台帳なし) | `71 green, **58 red**` **exit 1** | `108 green, **0 red**` **exit 0** |

**これは本走行が新しく入れた赤である。** 番兵が `hidden` と裁いて 58 門を倒す。

**だが実害は測定で 0 である**:
- (a) CI の checkout は兄弟倉を作らない —— **§2-1 で実測**。
- (b) `tribunal.yml` の **52 step** を読んだが、兄弟倉を作る step は一つも無い。
- (c) CI の 8 門を実際に撃っても兄弟倉は生まれなかった(実測)。
- (d) そして**この赤は設計上の意図どおりの赤である** —— 文面が「隠蔽の形」を名指す。

**ゆえに欠陥ではなく、名を持つべき残債である。** `SM-V3` と名付ける(§4)。

### 2-3.【新発見 SM-V2】外の凍結門は「撃ちが実行されていること」を検めていない

**誰も撃っていない軸**がもう一つ在った。reflect は「門が無罪と宣言した形」を一番手に挙げたが、
**「門が『撃った』と名乗る撃ちを、名乗りを残したまま実行だけ殺せるか」**は誰も撃っていない。

複製 `vj2` で、**literal を残したままループだけを空にする**変異を撃った。

```
❌ SILENT | N-1 bad3 の全 assert を殺す(逆側の凍結が丸ごと消える)        | exit=0 red=0
❌ SILENT | N-3 inFunc の撃ちだけ殺す(literal は残す)                  | exit=0 red=0
❌ SILENT | N-4 ghosts(G4 六形)の撃ちを殺す(literal は残す)           | exit=0 red=0
🔔 CAUGHT | N-2 N-1 + 実装も旧に戻す(盲点の完全復活を一括で)            | exit=1 red=1
```

原因は外の凍結門が **本文を grep するだけ**だからである:

```js
for (const form of ['f-global.js', 'f-exports.js', 'f-reflect.js', 'f-assign.js']) {
  assert.ok(body.includes(form), …);      // ← 文字列が在るかを見るだけ
}
```

`for (const … of inFunc)` を `of []` にしても `body.includes('f-global.js')` は真のままである。

**公平に書く。これは rework が開けた穴ではない。**
同じ形が **verify 相が建てた `ghosts`(G4 六形)にも在る**(N-4 が SILENT)。
**本走行全体の「外から凍らせる」作法の限界**であり、
第44条 c の教訓「**名を持つ定数を凍らせるだけでは、その定数を誰も渡していないことを捕まえられない**」の
**一段下の層**である —— 凍らせた定数は渡されているが、**渡した先のループが空**でも誰も鳴らない。

**そして N-2 が CAUGHT であることが重要である**: 撃ちを殺した上で**実装まで旧に戻す**——
つまり「盲点を実際に再成文化する」形は捕まる。**SILENT なのは「門の歯が抜けた」だけの状態であり、
病そのものが戻る形は今日も鳴る。実害は 0。** ゆえに残債 `SM-V2`。

### 2-4.【新発見 SM-V1】走行帳 13 枚の内部矛盾 —— **文書表の行数が三件ずれている**

`README.md` §10 の文書表の主張と `wc -l` の実測を全行照合した。

| 文書 | 主張 | 実測 | |
|---|---:|---:|---|
| findings / requirements / design / build / prove / review / security / docs / reflect | 730 / 718 / 1335 / 458 / 511 / 756 / 1072 / 455 / 605 | 同左 | ✅ **10 件一致** |
| **`verify.md`** | **704** | **717** | ⚠️ |
| **`PR.md`** | **343** | **388** | ⚠️ |
| **`rework.md`** | **—** | **703** | ⚠️ 未記入 |
| **`conclave.json`** | **—** | **560** | ⚠️ 未記入 |

**rework が R-2 で『走行帳の数を実測に揃えた』と名乗った、その当の表に、
rework 自身が書き足した後の数が入っていない。** `verify.md` は rework が註記を足したので 704→717 に伸び、
`PR.md` は rework が §1 に註記と §9 の案内を足したので 343→388 に伸びた。

**これは嘘ではなく、置き去りである。** 散文の行数は PR を読む者の判断を一ミリも変えない。
**軽微であり、残債 `SM-V1` として名を持たせる。**

### 2-5. 新設 21 門のうち一本も読まれていない門は無いか → **0 本。ただし 7 本は薄い。**

`git diff | grep "^+test("` = **21 本**。各門の名の核を走行帳 13 枚に機械で grep した。

```
誰も名を挙げていない門 = 0 / 21
2 枚以下しか触れていない門 = 7 / 21
```

薄い 7 本(うち **4 本は `PR.md` 一枚のみ**):

| 門 | 触れた文書 |
|---|---|
| `gauge(番兵): 一度 null を名乗った後に台帳が現れたら鳴る` | PR.md のみ |
| `gauge(番兵): 倒れるとき「どの門が汚したか」を名指す` | PR.md のみ |
| `gauge(CLI): --json は欄を列挙する` | PR.md のみ |
| `gauge(番兵): 「読めない」は「不在」ではない` | PR.md / verify.md |
| `gauge(CLI): healable は exit code と必ず一致する` | PR.md / verify.md |
| `gauge(CLI): 読まれずに消える行を監査が名乗る` | PR.md / verify.md |
| `gauge(並行): 競合の裁定は純関数であり、門から呼ばれている` | PR.md / verify.md |

**security が「7 本を一行も読んでいない」と自白したが、その 7 本は
『誰も読んでいない』のではなく『verify 相が建て、PR.md の表にしか名が無い』**という状態である。
**しかもこの 7 本はすべて `verify.md` に建てた記録が在る。**
第16条の要求(見落としと意図した残債は別物)は満たされている。**欠陥ではない。**

---

## 3. なぜ REWORK ではないのか

**reflect の三件(R-1/R-2/R-3)はすべて閉じた。私が自分で撃って確かめた。**

| # | reflect の告発 | 私の実測 |
|---|---|---|
| **R-1** | 静的の門が建てられた当の病に無音 / `trap2` が盲点を成文化 | **四形 4/4 CAUGHT。退行四形も CAUGHT。`trap2` から当該行が消え `bad3` が逆側を凍結。偽陽性 +0。** |
| **R-2** | 無音率が母集団を跨いで並んでいる | **§1 の表の当該行に ⚠️ 註記が直接入り、§9 が「§7 は急いでいる人ほど読むべき節」に変わった**(PR.md 実物を読んで確認) |
| **R-3** | SM-S2 の理由が測定で偽 | **塞がっている。`hidden` は実機で返る。理由は「振替の機では分けられない」に書き直された** |

そして **reflect が「私が見ていないこと」に挙げた最重量の限界(CI)を、私が解消した。**

**新たに見つけた三件は、いずれも:**
- **CI 形でも神の機でも exit 0** —— 実害が測定で 0。
- **名と理由と払う条件を書ける** —— 残債の要件を満たす。
- **一巡を要さない** —— 散文への追記で閉じる(SM-V1)か、次走行の単独課題である(SM-V2/V3)。

> **教主の方針は「この走行は一つの PR で閉じる。残債は名を付けて次の走行へ送る」である。**
> **三件は名を付けて送るべきものであり、PR を止める理由ではない。**

### なぜ BLOCK ではないのか

- 設計の誤りが一件も無い。**致命 4 件は塞がり**(reflect が実測、私は 1-3 で退行側から再確認)、
  **exit の規約は一文字も動かず**、**実台帳は不変**、**秘密の混入 0**、**engine 39 本すべて読み込み可**。
- CI 相当の門 12 本すべて exit 0。`critic.js review graph --self` は 107 件の教訓で裁いて **nothing**。

---

## 4. 次の走行へ送る残債 —— **私が名を付けた三件**

既存の 14 件(`PR.md` §6 が正本)に加えて、本相が測って名付ける。**残債は 17 件になる。**

### `SM-V1` 走行帳 `README.md` §10 の文書表の行数が三件ずれている
- **実体**: `verify.md` 704→実測 717 / `PR.md` 343→実測 388 / `rework.md` と `conclave.json` が `—`。
- **なぜ今やらないか**: **やってよい。代は 4 行の書き換えであり 0 分である。**
  だが**私は判定者であって構築者ではない**(第11条)—— 散文を直すのは教主か次の相の権能である。
  **私が直せば「判定者が自分の裁定の対象を書き換えた」ことになる。**
- **払う条件**: 教主が PR を出す前に 4 行を実測値に直す。**この PR の中で閉じてよい。**

### `SM-V2` 外の凍結門は「撃ちが実行されていること」を検めていない
- **実体**: `inFunc` / `ghosts` / `bad3` の撃ちを空ループにすると **exit=0 red=0**(実測)。
  外の門は `body.includes(…)` で本文を grep するだけである。
- **なぜ今やらないか**: **本走行が新しく作った穴ではない**(verify 相の `ghosts` にも在る)。
  直すには「門が自分の assert 回数を数えて外に名乗る」機構が要り、**これは本走行の射程を超える**
  (第57条: 機構の追加を同じ PR に混ぜない)。**そして病そのものが戻る形(N-2)は今日も鳴る** ——
  この穴を通れるのは「門の歯を抜く」だけであって「盲点を再成文化する」ことはできない。
- **払う条件**: 撃ちのループが実際に回った回数を assert する形(`assert.strictEqual(shot, 4)`)を
  **本走行の外から**建てる単独 PR。**射程は本走行の 21 門に限らない** —— 楽園の全門に同じ病が在りうる。
  **reflect が「一番手」と名指した『門が無罪と宣言した形の層』の、兄弟にあたる層である。**

### `SM-V3` 兄弟倉が「在るが空」の機で 58 門が赤くなる
- **実体**: CI 形 + `mkdir ../paradise-creations` = `71 green / **58 red**` exit 1(baseline は 0 red / exit 0)。
- **なぜ今やらないか**: **測定で実害が 0 である**(第38条)。CI の checkout は兄弟倉を作らず(実測)、
  `tribunal.yml` 52 step のどれも作らない(全文を読んだ)。**そして赤の文面は設計どおり隠蔽を名指す。**
  「倉は在るが空」を skip に倒せば **SM-S2 で塞いだ穴が開き直る** —— これは**塞いだものを外す修理**である。
- **払う条件**: 「一度でも台帳が在った記録」を倉が持つ印(`.paradise-creations` 目印の中に世代を書く等)。
  **rework が SM-S2 の払う条件に書いたのと同じ印であり、二件は同じ一本の PR で払える。**

---

## 5. 私が見ていないこと(第16条)

**この節が本報告で最も重要である。** 上の裁定は以下の限界の上に立っている。

1. **私は本物の GitHub Actions(ubuntu)で一度も走らせていない。**
   測ったのは **Windows 11 / Node v24.14.0 で「CI と同じ形の checkout」を再現した機**である。
   OS が違えば `fs.statSync` の挙動・パス区切り・行末(CRLF)・`git` の振る舞いが違う。
   **「CI 形で緑」は「CI で緑」ではない。** ただし三つの理由でこれを裁定の根拠に使ってよいと判断した:
   (a) baseline も同じ機で同じ形で緑であり、**差分 +21 門だけが新しい**、
   (b) 落ちうる箇所(番兵の不在判定)は `workspace.resolve()` の `exists` 一つに集約されており、
       その値を**三通りの機で直接印字して確かめた**、
   (c) **PR を出せば CI が答える** —— そして本走行は PR で閉じる方針である。
   **もし CI が赤なら、それは SM-V3 の形(兄弟倉が在るが空)であり、原因は既に名を持っている。**
2. **`RACE_LEAD_MS = 400` が CI で足りるかは私も測っていない。** verify・security・reflect・rework に続いて
   **五人目である。** 私が閉じたのは R-4 の CI 適合だけであり、**並行門の助走は別の未測である。**
3. **私が撃った変異は 16 件である**(四形 5 + 退行 6 + 撃ち殺し 5)。
   discover の 60 / review の 65 / security の 55 に比べて**桁が少ない。**
   **私は「誰も見ていない層」を選んで少数を撃つ戦術を採った** —— 当たったのは選び方が当たったからであり、
   **網羅ではない。私が選ばなかった層について私は何も言えない。**
4. **無音率という数を私は出さない。** 私の 16 件は盲点を狙って選んだ偏った標本であり、
   率にすれば reflect が §3 で裁いたのと同じ罪を犯す。
5. **四形の再現と退行の撃ちは 131 門(gauge 節の絞り込み)である。** 全 492 門で撃っていない。
   ただし**「鳴った」の主張は絞り込みで十分に立つ**(見る門が少ないほど鳴りにくい方向)。
6. **`tests/paradise.test.js` の +1650 行のうち、私が読んだのは約 500 行である。**
   静的の門・番兵の器・`mutableGlobals` の全文・外の凍結門・`ledgerAbsenceKind`。
   **残り 17 本の門の本文は読んでいない。そこに穴があっても私は見つけられない。**
7. **`design.md`(1335 行)/ `build.md` / `prove.md` / `security.md`(1072 行)を読んでいない。**
   reflect と rework の要約を通してしか知らない。**§Z(決めきらなかったこと 7 件)を裁いていない。**
8. **第62条の条文 58 行を一度も変異で撃っていない。** `codex.js check` が exit 0(索引と本文が一致 62 条)を
   確かめただけである。
9. **`SM-V2` の射程を私は測っていない。** 「楽園の全門に同じ病が在りうる」と書いたが、
   **撃ったのは本走行の 3 箇所だけである。** 何本の門が同じ形かは知らない。
10. **trajectory 80 点は批准前の値である**(第27条)。`domainsRatified 5/6` / `phasesDone 10/11` は
    **本相が未完だから低い**のであって走行の荒れではない。**教主が `tribunal` を批准してから
    `gauge.js score --json` を撃ち直せば `complete:true` に上がる。**
    そして **`reworkCount: 0` はこの走行の実態より優しい数である** —— reflect→rework の差し戻しは
    conclave の rework 計数を経ずに行われた。**一巡の差し戻しは確かに在った。**
11. **私は `conclave.js ratify` を打っていない。** 批准は教主の権能である(第11条)。
12. **最初の tar 複製(`.git` 無し)で `hermetic` 系 3 門が赤くなった。**
    これは私の複製の欠陥だと判断し、本物の `git clone` で撃ち直した —— そちらは緑だった。
    **だが「`.git` が無い機で 3 門が赤くなる」こと自体を、私は走行の問題として掘っていない。**
    (CI は必ず `.git` を持つので実害は無いと判断した。)

---

## 6. 教主への申し送り

1. **PR を出してよい。** 一つの PR で閉じる方針どおりである。
2. **出す前に `SM-V1` の 4 行を直すことを推す**(§4)。代は 0 分であり、
   `README.md` §10 が「この走行の総目次」だからである。**私は判定者なので自分では直さない。**
3. **先に `conclave.js ratify tribunal` を打ち、それから `gauge.js score --json` を撃ち直せ**(第27条)。
   いま載っている 80 点は **verdict 相が未完のままの値**であり、批准後は `complete:true` に上がる。
4. **CI が赤くなったら、まず SM-V3 の形を疑え** —— 兄弟倉ディレクトリだけが存在する機である。
   その場合の原因も払い方も、既に名を持っている(§4)。**閾値を緩めるな。`|| true` を付けるな。**
5. **残債は 14 → 17 件になる。** 新しい三件(SM-V1 / SM-V2 / SM-V3)は §4 に名と理由と払う条件を書いた。

---

## 7. 結び

**この走行は、四度裏切られて四度応えた。**

prove が「25/25 無音ゼロ」と名乗り → review が別軸で SILENT 2。
verify が塞ぎ → security が別軸で致命 3。
verify が「6/6 CAUGHT / 偽陽性 0」と名乗り → reflect が別軸(深さ)で無音 4。
rework が四形を塞ぎ → **私が別軸(CI と、撃ちの実行)で二件。**

**教訓 `mutation-author-is-blind-to-own-net` は五度目も当たった。** 当たり続ける。
**だが本走行が他の走行と違うのは、裏切られるたびに、裏切った層に名を付けたことである。**

- 一度目の盲点は名を持たなかった。
- 二度目の盲点は名を持たなかった。
- 三度目の盲点(`trap2`)は**門に「無い」と宣誓させていた** —— 最悪の形である。
- 四度目の盲点(SM-L3 の 4 形)は**名を持ち、門が走行のたびに画面でその名を挙げる。**
- そして**五度目の私の二件も、いま名を持つ**(SM-V2 / SM-V3)。

第62条(a) が命じたのは「盲点を無くせ」ではない。**「言え」である。**

```
(a) 門を建てるとき、その門の形が何を見えなくするかを言え。
    言えない盲点は、存在しない盲点ではなく、まだ誰も落ちていない穴である。
```

**この走行は、自分が見えなくしているものを言えるようになった。**
それが 21 本の門の値打ちであり、**SHIP の理由である。**

---

*実測に使った複製: `$LOCALAPPDATA/Temp/vj`(tar 複製)/ `vj2`(tar 複製)/ `vj3`(tar 複製・`.git` 無し)/*
*`vj5`(**本物の git clone** + 未 commit / CI 形)/ `vj6`(**本物の git clone** HEAD 素 / baseline)—— すべて揮発。*
*変異走行 16 回。各回で `restored=true` と sha256 の一致を印字した。*
*`git checkout -- <file>` は一度も使っていない。commit も push もしていない。*
*実台帳 `955ea34a020bf5681b5607de9ed22abaddc7bc5228cb231dd843c05ba55aab77` / 7 行 —— 本相の前後で不変。*
*`graph/*.js` と `tests/*.js` に一行も書いていない。`CLAUDE.md` / `.env` / `tribunal.yml` に触れていない。*
*`conclave.js ratify` を打っていない —— 批准は教主の権能である。*
